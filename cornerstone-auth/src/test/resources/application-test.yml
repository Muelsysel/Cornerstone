# 测试专用：关闭 Nacos 注册，避免测试环境依赖本地 Nacos
spring:
  cloud:
    nacos:
      discovery:
        enabled: false
        register-enabled: false

# 与主配置一致：Feign 附加/UserContextFilter 校验用内部令牌
cornerstone:
  internal-token: cornerstone-internal-secret
