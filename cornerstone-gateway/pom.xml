<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>
    <parent>
        <groupId>com.cornerstone</groupId>
        <artifactId>cornerstone</artifactId>
        <version>1.0.0-SNAPSHOT</version>
    </parent>
    <artifactId>cornerstone-gateway</artifactId>
    <name>Cornerstone Gateway</name>
    <description>统一入口网关：路由转发、令牌校验、CORS</description>

    <dependencies>
        <!-- API 契约：服务名常量，路由禁止硬编码服务名 -->
        <dependency>
            <groupId>com.cornerstone</groupId>
            <artifactId>cornerstone-api</artifactId>
        </dependency>
        <!-- 公共模块契约（Result/ErrorCode） -->
        <dependency>
            <groupId>com.cornerstone</groupId>
            <artifactId>cornerstone-common</artifactId>
        </dependency>
        <!-- Spring Cloud Gateway（WebFlux，禁止引入 spring-boot-starter-web） -->
        <dependency>
            <groupId>org.springframework.cloud</groupId>
            <artifactId>spring-cloud-starter-gateway</artifactId>
        </dependency>
        <!-- Actuator：只暴露 health/info（application.yml management.endpoints） -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-actuator</artifactId>
        </dependency>
        <!-- Nacos 服务注册 -->
        <dependency>
            <groupId>com.alibaba.cloud</groupId>
            <artifactId>spring-cloud-starter-alibaba-nacos-discovery</artifactId>
        </dependency>
        <!-- 负载均衡：lb:// 路由解析 Nacos 实例所必需 -->
        <dependency>
            <groupId>org.springframework.cloud</groupId>
            <artifactId>spring-cloud-starter-loadbalancer</artifactId>
        </dependency>
        <!-- Caffeine：LoadBalancer 服务实例缓存（默认 SimpleCache 仅开发用，生产有警告） -->
        <dependency>
            <groupId>com.github.ben-manes.caffeine</groupId>
            <artifactId>caffeine</artifactId>
        </dependency>
        <!-- Redis Reactive：RequestRateLimiter 限流（基于 Redis） -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-data-redis-reactive</artifactId>
        </dependency>
        <!-- 资源服务器：JWT 解码（NimbusReactiveJwtDecoder） -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-oauth2-resource-server</artifactId>
        </dependency>
        <!-- Lombok -->
        <dependency>
            <groupId>org.projectlombok</groupId>
            <artifactId>lombok</artifactId>
            <scope>provided</scope>
        </dependency>

        <!-- 测试 -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-test</artifactId>
            <scope>test</scope>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <plugin>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-maven-plugin</artifactId>
                <executions>
                    <execution>
                        <goals>
                            <goal>repackage</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>
        </plugins>
    </build>
</project>
